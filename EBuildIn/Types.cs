namespace EBuildIn
{
    public enum Types
    {
        Empty,
        T,  // generic, intended for functions that allow multiple types (such as Console:Write(T))
        Boolean,
        Number,
        Text,
        List,
    }
}
